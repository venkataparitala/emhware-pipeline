# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      filename:
        description: "Filename to be created"
        required: true
        default: "test.txt"
      input_string:
        description: "Text to be written to file"
        required: true
        default: "Hello Wordl!"
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      - name: Execute SSH commmands on remote server
        uses: JimCronqvist/action-ssh@master
        with:
          hosts: 'user@domain.com'
          privateKey: ${{ secrets.PRIVATE_KEY }}
          command: |
            ls -lah
            ./file.sh ${{ inputs.filename }} ${{ inputs.input_string }}
  
      # - name: Execute file Script
      #   run: ssh ${{ secrets.REMOTE_HOST }}@${{ secrets.REMOTE_USER }} bash file.sh ${{ inputs.filename }} ${{ inputs.input_string }}

      # - name: Execute webserver Script
      #   run: ssh ${{ secrets.REMOTE_HOST }}@${{ secrets.REMOTE_USER }} bash webserver.sh
